@model List<Warriors_Clinic.Models.PhysicianAdvice>
 
@{
    Layout = "~/Views/Shared/_PatientLayout.cshtml";
}
<div class="w-100 d-flex justify-content-start mb-3">
    
    <button onclick="document.referrer ? history.back() : location.href='/Admin/Dashboard'"
            class="btn btn-light shadow rounded-circle p-2">
        🡨
    </button>

</div>
<h2>My Prescriptions</h2>
 
<table class="table table-bordered">
    <thead class="table-dark">
        <tr>
            <th>Advice</th>
            <th>Drug</th>
            <th>Dosage</th>
            <th>Timing</th>
 
        </tr>
    </thead>
 
    <tbody>
        @foreach (var advice in Model)
        {
            var prescriptions = advice.PhysicianPrescriptions?.ToList();
 
            if (prescriptions != null && prescriptions.Count > 0)
            {
                for (int i = 0; i < prescriptions.Count; i++)
                {
                    <tr>
                        @* Show advice only once *@
                        @if (i == 0)
                        {
                            <td rowspan="@prescriptions.Count">
                                @advice.Advice
                            </td>
                        }
 
                        <td>@prescriptions[i].Drug?.Tittle</td>
                        <td>@prescriptions[i].Dosage</td>
                        <td>@prescriptions[i].Timing</td>

                    </tr>
                }
            }
        }
    </tbody>
 
</table>